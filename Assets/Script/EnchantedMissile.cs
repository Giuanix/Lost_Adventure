using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnchantedMissile : MonoBehaviour
{
    /*public float Damage; //variablile per stabilire il danno

    private void OnCollisionEnter(Collision collision) //metodo per la gestione del proiettile e di come esso interagisce con l'ambiente
    {
        Destroy(gameObject);

        if (collision.transform.CompareTag("Enemy"))
        {
            collision.transform.GetComponent<EnemyHealth>().DecreaseHealth(Damage);
        }
    }*/
}
